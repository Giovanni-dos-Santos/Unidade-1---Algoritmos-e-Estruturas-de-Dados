#include <stdio.h>
#include <locale>
int main (){
	setlocale(LC_ALL, "Portuguese");
	int a=0, soma;
	while( a<10){
		printf("%d\n", soma= a + 1);
		a++;
	}
printf("\nSequência finalizada");
return 0;
}
