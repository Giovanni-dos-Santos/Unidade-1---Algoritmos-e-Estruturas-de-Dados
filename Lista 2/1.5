#include <stdio.h>
#include <locale>
int main (){
	setlocale(LC_ALL, "Portuguese");
	int a, b, c;
	printf("Digite um número inteiro:");
	scanf("%d", &a);
	printf("\nDigite um número inteiro:");
	scanf("%d", &b);
	printf("\nDigite um número inteiro:");
	scanf("%d", &c);
	if(a<b && a<c){
		printf("O número %d é o menor número", a);
	}
	else if(b<a && b<c){
		printf("O número %d é o menor número", b);
	}
	else
		printf("O número %d é o menor número", c);
	return 0;
}
