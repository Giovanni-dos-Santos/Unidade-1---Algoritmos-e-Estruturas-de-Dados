#include <stdio.h>
#include <locale>
int main(){
	setlocale (LC_ALL, "Portuguese");
	int soma=0;
    for (int i = 1; i <= 100; i++) {
        if (i % 2 != 0) {
            soma += i;
        }
    }
    printf("A soma dos numeros pares de 1 a 100 é: %d\n", soma);
    return 0;
}
