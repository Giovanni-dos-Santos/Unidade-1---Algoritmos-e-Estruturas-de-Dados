#include <stdio.h>
#include <locale>
int main(){
	setlocale(LC_ALL, "Portuguese");
	int i=1, soma=0;
	while(i <= 100){
		if (i%2==0){
			soma += i;
			printf("%d ", i);
		}
		i++;	
	}
	printf("\nA soma dos pares até 100 é: %d", soma);
	return 0;
}
