#include <stdio.h>
#include <locale>
int main(){
	setlocale (LC_ALL, "Portuguese");
    for (int i = 1; i <= 100; i++) {
        if (i % 6 != 0) {
            printf("%d ", i);
        }
    }
    return 0;
}
