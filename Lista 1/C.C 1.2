#include <stdio.h>
#include <locale>
int main(){
	setlocale (LC_ALL, "Portuguese");
	float a;
	printf("Escreva um número real: ");
	scanf("%f", &a);
	printf("O número inteiro é %.2f", a);
	return 0;
}
